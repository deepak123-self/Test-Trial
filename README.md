# Test-Trial

Test repository.

## Test folder structure

The repository now includes a basic test directory layout to organize test files by type:

- `tests/smoke/` - quick validation tests
- `tests/regression/` - broader regression coverage
- `tests/object/` - object-specific test files
